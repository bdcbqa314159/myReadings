# binaries ship in via ~/work/bookward/scripts/dist.sh ~/work/myReadings/bin
bin/

# the repo is PUBLIC; published = the generated PDF + Markdown catalog.
# The log itself stays local; .tex is an intermediate artifact.
reading.db
reports/*.tex

# latexmk work files
reports/*.aux
reports/*.log
reports/*.out
reports/*.fls
reports/*.fdb_latexmk
reports/*.synctex.gz

.DS_Store
CLAUDE.local.md
